{
  "name": "meetingroom",
  "version": "1.0.0",
  "description": "Meeting Room Reservation System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "dev": "nodemon server.js",
    "pm2": "pm2 start server.js --name meeting-room-server",
    "pm2:stop": "pm2 stop meeting-room-server",
    "pm2:restart": "pm2 restart meeting-room-server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
} 